// Widget test for Veera's Beauty & Tattoo Studio app
import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test placeholder', (WidgetTester tester) async {
    // Firebase initialization required at runtime — skipping full widget test
    expect(true, isTrue);
  });
}
